fn apply_pure(n: i32, arr: &mut [i32], f: pure fn (i32) -> i32) {
    let mut i = 0;
    while (i < n) {
        arr(i) = f(arr(i));
        i += 1;
    }
}

fn bar(n: i32, arr: &mut [i32]) {
    apply_pure(n, arr, pure | x | x);
    apply_pure(n, arr, pure @ | x | x);
}