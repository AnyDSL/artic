struct S(i32, i32, i32);
struct T;

#[export]
fn a() = S(1, 2, 3);

#[export]
fn b(s: S) = match s {
  S(x, y, z) => x + y + z
};

#[export]
fn c() = T();

#[export]
fn d(t: T) = match t {
  T => true
};