fn test(c: bool) -> i32 {
    let u: u64 = 2;
    let _ = if c { 1 } else { u };
    let _ = if c { u } else { 1 };
    let _ = if c { 1 } else { return(1) };
    let _ = if c { return(1) } else { 1 };
    let _ = if c { 1 } else { 1.0 };
    let _ = if c { 1.0 } else { 1 };
    let _ = if c { 1.0 } else { 1 };
    1
}

