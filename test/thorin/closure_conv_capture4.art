fn run(f: fn(i32) -> i32, x: i32) {
    f(x)
}

fn foo(x: i32, y: &mut i32) -> i32 {
    if (x == 0)
        { x + *y }
    else
        { 9 }
}

#[export]
fn bar(x: i32, y: &mut i32) -> i32 {
    foo(x + 1, y);
    *y
}