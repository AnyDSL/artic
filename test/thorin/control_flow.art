#[export]
fn test_branch(x: i32) -> i32 {
    if (x > 0) {
        1 / x
    } else {
        0
    }
}

#[export]
fn test_switch(x: i32) -> i32 {
    match (x) {
        0 => x,
        1 => 1,
        _ => 1 / x
    }
}