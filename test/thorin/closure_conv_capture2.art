#[import(cc="C")]
fn bar() -> ();

// similar to the last one but now we capture a 'break' continuation
#[export]
fn foo(f: fn(i32) -> i32) -> i32 {
  while true {
     if (f(5) > 6) { break() }
  }
  
  0
}
